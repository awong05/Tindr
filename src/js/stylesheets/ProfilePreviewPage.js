import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center'
  },
  footer: {
    flexDirection: 'row'
  },
  image: {
    borderRadius: 100,
    height: 200,
    width: 200
  }
});
